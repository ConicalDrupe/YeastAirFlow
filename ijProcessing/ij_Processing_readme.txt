#
#
blurDetect.py work in progress -- designed to take in a photograph, find out a blurry coefficient via FFT. And label its EXIF meta data
#
renameSpecies.sh reads all images in a folder and renames them based on the write-directory. It also labels it EXIF meta data to its species.
#processImagesToCSB_.ijm is a FIJI script that reads a folder of images and analyzes particles to out put physical cell parameters in a csv for each image. _These csv's need to be appended and stored in a data base at the next step.
